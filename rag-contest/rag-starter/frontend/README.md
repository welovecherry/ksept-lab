# frontend

React + Vite chat UI. Same shape as the Foundations chat, with a Sources line under each assistant message.

## Setup

```bash
cd frontend
npm install
npm run dev
# Running on http://localhost:5173
```

Make sure the backend is running on port 5000 in another terminal.
